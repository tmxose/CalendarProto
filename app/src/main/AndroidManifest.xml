<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GCalendars"
        tools:targetApi="31">

        <activity
            android:name=".LogIn.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LogIn.RegisterActivity" />

        <activity android:name=".MainActivity" />
        <activity android:name=".custom.CustomCalendar" />
        <activity
            android:name=".custom.AddEvent"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".custom.CalendarAdapter" />
        <activity android:name=".custom.CalendarViewHolder" />
        <activity android:name=".personalSettings" />
        <activity android:name=".CreateGroupCalendarActivity"/>
        <activity android:name=".DeepLinkActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="groupcalendars.infinityfreeapp.com" />
            </intent-filter>
        </activity>
    </application>
</manifest>